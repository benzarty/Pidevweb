{% extends 'base.html.twig' %}
{% block body %}



        <h1>{{ article.id }}</h1>
        <h1>{{ article.title }}</h1>
        <!-- <h1>{{ article.date }}</h1> -->
        <h1>{{ article.etat }}</h1>
        <h1>{{ article.recl}}</h1>

        <hr>
        <a href="/">Retour</a>


{% endblock %}