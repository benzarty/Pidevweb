{% extends 'base.html.twig' %}
{% block body %}

    <!-- Content here -->


    {% if articles %}
        <table id="articles" class="table  table-bordered table-hover table-dark   ">
            <thead>
            <tr>
                <th>Id Professeur</th>
                <th>Nom</th>
                <th>Prenom</th>
                <th>email </th>
                <th>password</th>
                <th>specialite</th>
                <th>Profil</th>

                <th>Action</th>

            </tr>
            </thead>
            <tbody>
            {% for article in articles %}
                <tr>
                    <td>{{ article.idprofesseur }}</td>
                    <td>{{ article.nom }}</td>
                    <td>{{ article.prenom }}</td>
                    <td>{{ article.email }}</td>
                    <td>{{ article.password }}</td>
                    <td>{{ article.specialite }}</td>
                    <td>{{ article.profil }}</td>






                    <td>
                        <a href="/ProfInfo/{{ article.idprofesseur }}" class="btn btn-secondary">Détails</a>
                        <a href="/Prof/edit/{{ article.idprofesseur }}" class="btn btn-info">Modifier</a>
                        <a href="/Prof/delete/{{ article.idprofesseur }}" class="btn btn-danger"
                           onclick="return confirm('Etes-vous sûr de supprimer cet article?');">Supprimer</a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    {% else %}
        <p>Aucun articles</p>
    {% endif %}

{% endblock %}