<form method="post" action="{{ path('evenement_delete', {'idEvenement': evenement.idEvenement}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ evenement.idEvenement) }}">
    <button class="btn">Delete</button>
</form>
